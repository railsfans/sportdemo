<script>
/* 
	author: istockphp.com
*/
jQuery(function($) {
		chart1 = new Highcharts.Chart({  
                     chart : {  
                            renderTo: 'highchartcontainer',  
                            type : 'bar'  
                     },  
                     title : {  
                            text : 'Fruit Consumption'  
                     },  
                     xAxis : {  
                            categories: [ 'Apples', 'Bananas', 'Oranges' ]  
                     },  
                     yAxis : {  
                            title : {  
                                   text: 'Fruit eaten'  
                            }  
                     },  
                     series : [ {  
                            name : 'Jane',  
                            data : [1, 0, 4 ]  
                     }, {  
                            name : 'John',  
                            data : [5, 7, 3 ]  
                     } ]  
              });
	$("a.topopup").click(function() {
			loading(); // loading
			setTimeout(function(){ // then show popup, deley in .5 second
				loadPopup(); // function show popup 
			}, 500); // .5 second
	return false;
	});

        $("a.topopup_step").click(function() {
			loading(); // loading
			setTimeout(function(){ // then show popup, deley in .5 second
				loadPopup_step(); // function show popup 
			}, 500); // .5 second
	return false;
	}); 

        $("a.topopup_distance").click(function() {
			loading(); // loading
			setTimeout(function(){ // then show popup, deley in .5 second
				loadPopup_distance(); // function show popup 
			}, 500); // .5 second
	return false;
	}); 

        $("a.topopup_calories").click(function() {
			loading(); // loading
			setTimeout(function(){ // then show popup, deley in .5 second
				loadPopup_calories(); // function show popup 
			}, 500); // .5 second
	return false;
	}); 


	/* event for close the popup */
	$("div.close").hover(
				function() {
					$('span.ecs_tooltip').show();
				},
				function () {
					$('span.ecs_tooltip').hide();
					}
			);
	
	$("div.close").click(function() {
		disablePopup();  // function close pop up
                disablePopup_step();
                disablePopup_distance();
                disablePopup_calories();
	});
	
	$(this).keyup(function(event) {
		if (event.which == 27) { // 27 is 'Ecs' in the keyboard
			disablePopup();  // function close pop up
		}  	
	});

        $(this).keyup(function(event) {
		if (event.which == 27) { // 27 is 'Ecs' in the keyboard
			disablePopup_step();  // function close pop up
		}  	
	});     

         $(this).keyup(function(event) {
		if (event.which == 27) { // 27 is 'Ecs' in the keyboard
			disablePopup_distance();  // function close pop up
		}  	
	});      
 
        $(this).keyup(function(event) {
		if (event.which == 27) { // 27 is 'Ecs' in the keyboard
			disablePopup_calories();  // function close pop up
		}  	
	});      

	
	$("div#backgroundPopup").click(function() {
		disablePopup();  // function close pop up
	});

        $("div#backgroundPopup_step").click(function() {
		disablePopup_step();  // function close pop up
	});

        $("div#backgroundPopup_distance").click(function() {
		disablePopup_distance();  // function close pop up
	});

        $("div#backgroundPopup_calories").click(function() {
		disablePopup_calories();  // function close pop up
	});

	
	$('a.livebox').click(function() {
		alert('Hello World!');
	return false;
	});
	

	 /************** start: functions. **************/
	function loading() {
		$("div.loader").show();  
	}
	function closeloading() {
		$("div.loader").fadeOut('normal');  
	}
	
	var popupStatus = 0; // set value
	
	function loadPopup() { 
		if(popupStatus == 0) { // if value is 0, show popup
			closeloading(); // fadeout loading
			$("#toPopup").fadeIn(0500); // fadein popup div
			$("#backgroundPopup").css("opacity", "0.7"); // css opacity, supports IE7, IE8
			$("#backgroundPopup").fadeIn(0001); 
			popupStatus = 1; // and set value to 1
		}	
	}
		
	function disablePopup() {
		if(popupStatus == 1) { // if value is 1, close popup
			$("#toPopup").fadeOut("normal");  
			$("#backgroundPopup").fadeOut("normal");  
			popupStatus = 0;  // and set value to 0
		}
	}

        var popupStatus_step = 0; // set value
	
	function loadPopup_step() { 
		if(popupStatus_step == 0) { // if value is 0, show popup
			closeloading(); // fadeout loading
			$("#toPopup_step").fadeIn(0500); // fadein popup div
			$("#backgroundPopup_step").css("opacity", "0.7"); // css opacity, supports IE7, IE8
			$("#backgroundPopup_step").fadeIn(0001); 
			popupStatus_step = 1; // and set value to 1
		}	
	}
		
	function disablePopup_step() {
		if(popupStatus_step == 1) { // if value is 1, close popup
			$("#toPopup_step").fadeOut("normal");  
			$("#backgroundPopup_step").fadeOut("normal");  
			popupStatus_step = 0;  // and set value to 0
		}
	}

         var popupStatus_distance = 0; // set value
	
	function loadPopup_distance() { 
		if(popupStatus_distance == 0) { // if value is 0, show popup
			closeloading(); // fadeout loading
			$("#toPopup_distance").fadeIn(0500); // fadein popup div
			$("#backgroundPopup_distance").css("opacity", "0.7"); // css opacity, supports IE7, IE8
			$("#backgroundPopup_distance").fadeIn(0001); 
			popupStatus_distance = 1; // and set value to 1
		}	
	}
		
	function disablePopup_distance() {
		if(popupStatus_distance == 1) { // if value is 1, close popup
			$("#toPopup_distance").fadeOut("normal");  
			$("#backgroundPopup_distance").fadeOut("normal");  
			popupStatus_distance = 0;  // and set value to 0
		}
	}

         var popupStatus_calories = 0; // set value
	
	function loadPopup_calories() { 
		if(popupStatus_calories == 0) { // if value is 0, show popup
			closeloading(); // fadeout loading
			$("#toPopup_calories").fadeIn(0500); // fadein popup div
			$("#backgroundPopup_calories").css("opacity", "0.7"); // css opacity, supports IE7, IE8
			$("#backgroundPopup_calories").fadeIn(0001); 
			popupStatus_calories = 1; // and set value to 1
		}	
	}
		
	function disablePopup_calories() {
		if(popupStatus_calories == 1) { // if value is 1, close popup
			$("#toPopup_calories").fadeOut("normal");  
			$("#backgroundPopup_calories").fadeOut("normal");  
			popupStatus_calories = 0;  // and set value to 0
		}
	}
	
	
	
	/************** end: functions. **************/
}); // jQuery End

</script>
<style>
<style>
/* 

	author: istockphp.com

*/

#backgroundPopup { 

	z-index:1;

	position: fixed;

	display:none;

	height:100%;

	width:100%;

	background:#000000;	

	top:0px;  

	left:0px;

}

#toPopup {

	font-family: "lucida grande",tahoma,verdana,arial,sans-serif;

    background: none repeat scroll 0 0 #FFFFFF;

    border: 10px solid #ccc;

    border-radius: 3px 3px 3px 3px;

    color: #333333;

    display: none;

	font-size: 14px;

    left: 50%;

    margin-left: -402px;

    position: fixed;

    top: 10%;

    width: 800px;

    z-index: 2;

}
#backgroundPopup_step { 

	z-index:1;

	position: fixed;

	display:none;

	height:100%;

	width:100%;

	background:#000000;	

	top:0px;  

	left:0px;

}

#toPopup_step {

	font-family: "lucida grande",tahoma,verdana,arial,sans-serif;

    background: none repeat scroll 0 0 #FFFFFF;

    border: 10px solid #ccc;

    border-radius: 3px 3px 3px 3px;

    color: #333333;

    display: none;

	font-size: 14px;

    left: 50%;

    margin-left: -402px;

    position: fixed;

    top: 10%;

    width: 800px;

    z-index: 2;

}

#backgroundPopup_distance { 

	z-index:1;

	position: fixed;

	display:none;

	height:100%;

	width:100%;

	background:#000000;	

	top:0px;  

	left:0px;

}

#toPopup_distance {

	font-family: "lucida grande",tahoma,verdana,arial,sans-serif;

    background: none repeat scroll 0 0 #FFFFFF;

    border: 10px solid #ccc;

    border-radius: 3px 3px 3px 3px;

    color: #333333;

    display: none;

	font-size: 14px;

    left: 50%;

    margin-left: -402px;

    position: fixed;

    top: 10%;

    width: 800px;

    z-index: 2;

}

#backgroundPopup_calories { 

	z-index:1;

	position: fixed;

	display:none;

	height:100%;

	width:100%;

	background:#000000;	

	top:0px;  

	left:0px;

}

#toPopup_calories {

	font-family: "lucida grande",tahoma,verdana,arial,sans-serif;

    background: none repeat scroll 0 0 #FFFFFF;

    border: 10px solid #ccc;

    border-radius: 3px 3px 3px 3px;

    color: #333333;

    display: none;

	font-size: 14px;

    left: 50%;

    margin-left: -402px;

    position: fixed;

    top: 10%;

    width: 800px;

    z-index: 2;

}



div.loader {

    background: url("loading.gif") no-repeat scroll 0 0 transparent;

    height: 32px;

    width: 32px;

	display: none;

	z-index: 9999;

	top: 40%;

	left: 50%;

	position: absolute;

	margin-left: -10px;

}

div.close {

    background: url("closebox.png") no-repeat scroll 0 0 transparent;

    bottom: 24px;

    cursor: pointer;

    float: right;

    height: 30px;

    left: 27px;

    position: relative;

    width: 30px;

}

span.ecs_tooltip {

    background: none repeat scroll 0 0 #000000;

    border-radius: 2px 2px 2px 2px;

    color: #FFFFFF;

    display: none;

    font-size: 11px;

    height: 16px;

    opacity: 0.7;

    padding: 4px 3px 2px 5px;

    position: absolute;

    right: -62px;

    text-align: center;

    top: -51px;

    width: 93px;

}

span.arrow {

    border-left: 5px solid transparent;

    border-right: 5px solid transparent;

    border-top: 7px solid #000000;

    display: block;

    height: 1px;

    left: 40px;

    position: relative;

    top: 3px;

    width: 1px;

}

div#popup_content {

    margin: 4px 7px;

}


div#popup_content_step {

    margin: 4px 7px;

}

div#popup_content_distance {

    margin: 4px 7px;

}
div#popup_content_calories {

    margin: 4px 7px;

}

</style>
</style>
<li style="padding: 1px; margin: 0px; width: 9%; border-radius: 20px 20px 20px 20px; background-color: #FF0099" class="li"><a href="#" class="topopup_step"><%= image_tag('zoom.png') %></a></li>
 <div class="loader"></div>
<p class="panel-content"> 
		<div id="toPopup_step"> 	
			<div class="close"></div>
			<span class="ecs_tooltip">Press Esc to close <span class="arrow"></span></span>
			<div id="popup_content_step"> <!--your content start-->
				<p>Step</p>
				<p>
				<div id="bar" style=" width: 700px; height: 100px; margin: 0 auto"></div>
				</p>
				<p>
			 <div id="highchartcontainer" style=" width: 700px; height: 300px; margin: 0 auto"></div>
			   </p>
			</div> <!--your content end-->
		</div> <!--toPopup end-->
		<div id="backgroundPopup_step"></div>
		</p> 
